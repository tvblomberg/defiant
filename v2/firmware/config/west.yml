manifest:
  remotes:
    - name: zmkfirmware
      url-base: https://github.com/zmkfirmware
  projects:
    - name: zmk
      remote: zmkfirmware
      revision: main
      import: app/west.yml
  self:
    # Full path from the repo root, not just "config". See v1/firmware/config/west.yml.
    path: v2/firmware/config
